export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // Eslint-disable-next-line and no-constant-condition
    if (true) {
      resolve();
    } else {
      reject();
    }
  });
}